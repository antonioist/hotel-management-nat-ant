<%= simple_form_for [@room, @hotel] do |f| %>
  <%= f.input :number, label: "Add the number of the room", hint: "12, 15, 16", input_html: { type: 'number' }%>
  <%= f.button :submit, class: "button" %>
<% end %>